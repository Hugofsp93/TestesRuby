<div class="card">
  <div class="card-header">
    <h2>Log in</h2>
    <ul class="actions">
      <li>
          <a href="" title="<%= t('Refresh') %>">
              <i class="zmdi zmdi-refresh-alt"></i>
          </a>
      </li>
    </ul>
</div>
  <div class="card-body card-padding">
    <%= semantic_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <%= f.inputs do %>
        <%= f.input :email %>
        <%= f.input :password %>
        <%= f.input :remember_me, as: :boolean %>
      <% end %>

      <%= f.actions do %>
        <%= f.action :submit, label: "Log in", :as => :button, :button_html => { class: "btn btn-primary btn-sm m-t-10 waves-effect" } %>
        <%#= render "devise/shared/links" %>
        <%= link_to "Cadastre-se", new_user_path, style: "position: relative; top: 13px; left: 11px;" %><br />
        <%= link_to "Esqueceu a senha?", new_password_path(resource_name), style: "position: relative; top: -4px; left: 100px;" %><br />
      <% end %>
    <% end %>
	</div>
</div>
